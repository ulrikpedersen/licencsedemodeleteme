# licencsedemodeleteme
nothing but an apache 2 license

```C++

// Demo syntax highlight
class Rectangle {
    int width, height;
  public:
    void set_values (int,int);
    int area (void);
} rect;
```
